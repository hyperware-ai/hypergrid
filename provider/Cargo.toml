[profile.release]
lto = true
opt-level = "s"
panic = "abort"

[workspace]
members = [
    "provider",
    "target/caller-utils",
    "target/caller-util?",
    "target/provider-caller-util?",
]
resolver = "2"
